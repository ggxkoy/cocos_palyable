# 试玩广告脚本：{名称}

> Schema 对标 `docs/design/reference/三冰-试玩广告脚本-模拟经营+策略.pdf`（生产级脚本深度），
> 由 `/video-to-design` 生成，交给 `/design-to-playable` 或美术/地编按章节直接开工。
> 填不出来的格留 {待定} 并注明依据缺失，不允许编造。
>
> **策划案是事实来源**：落地后任何玩法/数值改动都先改本文再改代码。
> 下面十个章节一个都不能少（`npm run lint:design` 会检查章节完整性与数值一致性）。

## 封面

- **玩法卖点**：{如 策略-塔防 / 模拟经营 / 打捞防线}
- **大纲**（5-6 条，一条一句）：
  1. {先引入什么玩法，玩家第一步和什么交互}
  2. {玩法如何递进}
  3. {压力/需求如何加大}
  4. {最终目标是什么；失败条件（如有）}
  5. {失败后是否可重新挑战、次数}

## 参考视频

| 文件/链接 | 时长/规格 | 备注 |
| --- | --- | --- |
| {reference/incoming/xxx.mp4} | {56s 竖屏 360x640} | {关键帧目录 docs/design/<名称>-frames/} |

## 分析结论（流水线保留章节）

- **目标结构（动机轴）**：{战斗压力型（收集→转化战斗力→战斗验证）/ 解压经营型（小循环嵌套大循环）/ 混合，判断依据}
- **操作归属**：{主角=虚拟摇杆（按下出现、拖动移动、松手即停）；范围自动交互清单；哪些自动化是购买获得的；禁止自动结算/自动走位}
- **目标链**：{列出所有目标及出现时机；每个循环闭合时下一目标必须已在场上。**非线性原则**：目标牌全部同时在场、付得起哪个买哪个，顺序由价格大小关系自然引导而非程序强制}
- **长期锚点与地图尺度**：{开局即见的远期目标及位置；角色高度占画面比例 → 地图分层}
- **模板映射（操作轴）**：{2D 模板名 或 3D 模块清单（modules/xxx + playables/xxx）}

## 资源循环

- **核心资源**：{资源A（来源），资源B（来源）}
- **循环 1（开局即在）**：{资源→加工→产出→用途 流水线，一步步写}
- **循环 2（解锁后出现，地编预留空位）**：{...}
- **战斗循环**（战斗压力型）：{击杀敌人→掉落→建造/升级}
- **核心逻辑**：{操作重心从 A→B 的转移；压力从 X→Y 的转移}

## 角色&道具

### 角色表

| 角色名 | 说明/图片参考 | 角色功能 | 动作拆解 | 模型资产引用 |
| --- | --- | --- | --- | --- |
| 主角（玩家操控） | {参考帧} | {能做什么、和哪些建筑交互} | {跑步/攻击/锻造/背后堆叠…} | {assets/art/.../01_主角} |
| {NPC/雇员} | | | | |
| {敌人小兵} | | | | |
| {BOSS} | | | | |

### 道具表

| 道具名称 | 说明/图片参考 | 备注 |
| --- | --- | --- |
| {金币} | | |
| {资源/武器} | | |

## 地编需求

- **布局参考**：{整体布局描述/示意；分期出现的物件（如"外围一圈建筑在升级后出现，不可交互"）}

### 场景细节&交互逻辑

| 建筑/道具 | 说明（含交互与物理规则） | 参考图片 |
| --- | --- | --- |
| 整体环境 | {光照/阴影/地表要求} | |
| {围墙} | {碰撞规则、受击表现、升级后变化} | |
| {大门} | {开合触发条件} | |
| {生产建筑} | {自动/需人工、传送带走向、产物存放与采集方式} | |
| {防御建筑} | {驻守单位、弹药消耗与补充} | |
| {敌人死亡} | {倒地方式、掉落} | |

## 流程引导&数值设计

### 流程引导

| 时间节点 | 描述说明（含引导：箭头指向/脚下线路/停留触发） | 画面参考 |
| --- | --- | --- |
| 出生点 | | |
| {第一次交互后} | | |
| {每个解锁节点} | | |
| 游戏进程结束 | {结束条件；失败条件（如有）} | |

### 数值设计

先写**大小关系原则**（美术/程序可自行调绝对值但不许破坏关系）：
1. {终极目标需要最多金币}
2. {循环1 各解锁点递增关系}
3. {循环2 各解锁点递增关系}

| 交互点 | 需要金币数 | 备注 |
| --- | --- | --- |
| {箭塔} | {10} | |

属性数值：{携带/弹药上限、耐久度、单位伤害（小兵 1 / BOSS 10 这类相对关系）…}

### 敌人攻击机制（战斗压力型）

| 时间节点 | 描述说明 | 画面参考 |
| --- | --- | --- |
| 初始阶段 | {小兵生成与目标} | |
| {关键解锁后} | {BOSS 出现规则、数量上限、优先攻击逻辑} | |

## UI&引导按钮

| 需求描述 | 图片参考 | 备注 |
| --- | --- | --- |
| 未交互前引导拖曳控制主角 | | |
| 箭头指向建筑/资源；脚下线路引导 | | |
| 资源计数（位置/样式） | | |
| 交互方框（未站上白色/站上绿色） | | |
| 解锁按钮 + 投入进度条 | | |
| 血条规范（友绿敌红，无数值） | | |
| 警告表现（描边变红/红框闪烁） | | |
| 结算/失败界面（LOGO+按钮；语言要求） | | |

## 音效

| 音效描述 | 参考 | 备注 |
| --- | --- | --- |
| {采集声} | | |
| {攻击声} | | |

## 模板映射（→ 代码落点）

> **机器可校验**：`配置项` 一列写成目标 Config 的点路径并用反引号包住，`值` 必须与代码
> 实际值一致；正文里要出现目标 `assets/scripts/**/XxxConfig.ts` 路径。
> `npm run lint:design` 会逐项比对，字段被改名或数值被改动而本表没跟上即失败。
> 尚未落地的策划案（刚从视频产出）可以不写 Config 路径，校验会标记为 pending 跳过。

| 配置项 | 值 | 视频/脚本依据 |
| --- | --- | --- |
| `{config.dotted.path}` | {与代码一致的值} | {依据} |

- 模块清单：{modules/... 挑选与接线说明}
- 与模板的差异点（按优先级，标注本次落地项）：
  1. {...}
